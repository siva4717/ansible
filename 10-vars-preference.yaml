- name : variable preference
  hosts: local
  connection: local
  vars: 
    NAME: from play leval
  # vars_files:
  # - course.yaml
  vars_prompt:
  - name: NAME
    prompt: what is your name?
    private: false
  tasks:
  - name: from task leval
    # vars: 
    #   NAME: from task leval
    ansible.builtin.debug:
      msg: "{{ NAME }}"

# first preference command leval variables 
# second preference task leval
# third preference files leval
